import {
    HorizontalBox,
    VerticalBox,
} from "std-widgets.slint";

export component Search {
    VerticalBox {
        HorizontalBox {
            Text {
                text: "Search";
            }
        }
    }
}
