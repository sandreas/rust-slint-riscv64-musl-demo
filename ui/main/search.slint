import { Switch, VerticalBox, HorizontalBox, Palette } from "std-widgets.slint";


export component Search {
    VerticalBox {
        HorizontalBox {
            Text {
                text: "Search";
            }
        }
    }
}