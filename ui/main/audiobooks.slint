import {
    Button,
    HorizontalBox,
    Palette,
    ScrollView,
    Slider,
    Switch,
    VerticalBox,
} from "std-widgets.slint";

import {SlintAudioPlayer} from "../globals.slint";

export component Audiobooks {
    VerticalBox {
        HorizontalBox {
            Button {
                text: "play something";
                clicked => {
                    SlintAudioPlayer.play();
                }
            }
            Text {
                text: "Audio Books";
            }
        }
    }
}