import {
    Button,
    HorizontalBox,
    Palette,
    ScrollView,
    Slider,
    Switch,
    VerticalBox,
} from "std-widgets.slint";


export component Audiobooks {
    VerticalBox {
        HorizontalBox {
            Text {
                text: "Audio Books";
            }
        }
    }
}