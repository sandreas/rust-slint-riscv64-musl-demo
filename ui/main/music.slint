import {
    HorizontalBox,
    VerticalBox,
} from "std-widgets.slint";

export component Music {
    VerticalBox {
        HorizontalBox {
            Text {
                text: "Music";
            }
        }
    }
}
